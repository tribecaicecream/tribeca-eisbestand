name: Show all flavors in inventory list

on:
  push:
    paths:
      - '.github/workflows/show-all-flavors-inventory.yml'
      - 'scripts/show_all_flavors_inventory.py'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update inventory matrix
        run: python3 scripts/show_all_flavors_inventory.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "No changes"
            exit 0
          fi
          git commit -m "Show all flavors in inventory list"
          git push
